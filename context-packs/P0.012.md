# Ticket: P0.012
Spec: Fix duplicate dynamic texture lifecycle on five battle restarts [P1][P3]

## DONE-WHEN
A real Chromium run completes 5 BattleScene restart cycles; duplicate texture-key errors=0; uncaught page errors=0; each of 7 fixed dynamic keys exists exactly once after every cycle; usedJSHeapSize growth from post-cycle-1 GC to post-cycle-5 GC<=25MB

## Exact Owned Files
- src2/scenes/BattleScene.js
- scripts/verify-restart-textures.cjs

## Required Interface Signatures
- BattleScene.create
- BattleScene.createFog
- BattleScene.createBlightLayers

## Existing Failing Test & Command
Command: node scripts/verify-restart-textures.cjs
```
CURRENT FAILURE:
- Phaser's texture manager persists across BattleScene restarts.
- BattleScene creates fixed dynamic keys fog, vis, fog_mist, blight-t0, blight-t1, blight-ai-t0, and blight-ai-t1 without lifecycle cleanup.
- Existing shutdown removes only window key listeners.
- No real-browser gate proves five restart cycles have zero duplicate-key errors and <=25MB heap growth.
```

## Dependencies
- P0.001

## Diff Cap
400 lines

## Forbidden Scope
- Do not alter visual output, texture dimensions/content/filtering, game state, terrain, pathfinding, unit behavior, AI, balance, static asset loading, other scenes, config, git, or external systems
