# Ticket: P0.004
Spec: Replace direct-to-goal fallback after failed ground A* with nearest-reachable stop and explicit unreachable result [P1][P2]

## DONE-WHEN
Across 100 deterministic blocked-goal trials, solid-cell path entries === 0 and explicit unreachable feedback latency <= 100ms

## Exact Owned Files
- src2/engine/entity.js
- src2/engine/pathfinding.js
- scripts/verify-unreachable-fallback.cjs

## Required Interface Signatures
- Unit.repath
- NavGrid.findPath
- NavGrid.walkable

## Existing Failing Test & Command
Command: node scripts/verify-unreachable-fallback.cjs
```
CURRENT FAILURE: Unit.repath in src2/engine/entity.js assigns this.path=[{x:toX,y:toY}] when NavGrid.findPath returns null.
This direct fallback sends ground units toward an unreachable solid destination.
Required red harness: 100 deterministic blocked-goal trials; zero paths may enter solid cells; explicit unreachable feedback must emit within 100ms.
Expected current result: nonzero exit with DIRECT_SOLID_FALLBACK failure.
```

## Dependencies
- P0.003

## Diff Cap
400 lines

## Forbidden Scope
- Do not modify other game source, existing tests, images, planning docs except ticket state, config, git, or external systems
- preserve public signatures and do not change flying-unit behavior
