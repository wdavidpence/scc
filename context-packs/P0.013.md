# Ticket: P0.013
Spec: Correct production font URLs under both root and `/scc/` bases [P1]

## DONE-WHEN
Two Vite builds, base / and base /scc/, emit 0 unresolved-font warnings; real Chromium requests all 3 TTF files under each base with HTTP 200; font 404 count=0; document.fonts.check passes Orbitron 16px, Rajdhani 700 16px, and Rajdhani 600 16px under both bases

## Exact Owned Files
- index.html
- public/fonts/fonts.css
- scripts/verify-font-paths.cjs

## Required Interface Signatures
- index-font-link
- font-face-Orbitron
- font-face-Rajdhani

## Existing Failing Test & Command
Command: node scripts/verify-font-paths.cjs
```
CURRENT FAILURE:
- Every Vite build emits unresolved warnings for Orbitron-Variable.ttf, Rajdhani-Bold.ttf, and Rajdhani-SemiBold.ttf from inline index.html CSS.
- Font binaries exist at public/fonts/.
- Production base is /scc/ while development/root base is /; hard-coding either base would break the other.
- No dual-base browser network gate proves all three fonts return 200 and load successfully.
```

## Dependencies
- P0.001

## Diff Cap
400 lines

## Forbidden Scope
- Do not change font binaries, font families, weights, display behavior, non-font HTML/CSS, Vite base policy, JavaScript, gameplay, assets outside public/fonts/fonts.css, config, git, or external systems
