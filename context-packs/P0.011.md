# Ticket: P0.011
Spec: Invalidate A* and flow-field caches when destructible blockers open a route [P2][P3]

## DONE-WHEN
In 50 deterministic blocked-valley trials, 100% of 4 waiting A-star units and 4 shared-flow units per trial have complete legal open-valley routes after exactly 1 fixed 50ms simulation tick; stale flow fields=0; blocked route samples=0; invalidation latency<=50ms

## Exact Owned Files
- src2/scenes/BattleScene.js
- src2/engine/flowfield.js
- src2/engine/entity.js
- scripts/verify-blocker-invalidation.cjs

## Required Interface Signatures
- BattleScene.destroyRock
- FlowManager.invalidateNear
- FlowManager.ensure
- Unit.repath
- Unit.stepAlongFlow

## Existing Failing Test & Command
Command: node scripts/verify-blocker-invalidation.cjs
```
CURRENT FAILURE:
- BattleScene.destroyRock clears nav.blocked/nav.blockedBy but never calls the existing FlowManager.invalidateNear seam.
- Waiting A* units receive no topology-change signal and can remain on stale partial routes until timer-driven repath.
- Shared flow fields may remain stale for the 0.6-second rebuild interval.
- No deterministic golden replay proves all waiting ground units repath within one fixed simulation tick after a destructible opens a valley.
```

## Dependencies
- P0.003

## Diff Cap
400 lines

## Forbidden Scope
- Do not change terrain generation, unit stats, movement speed, pathfinding route algorithm, clearance, flying behavior, destruction visuals/audio, AI, balance, rendering assets, planning docs except state, config, git, or external systems
