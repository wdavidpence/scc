# BACKLOG

Noticed work is recorded here and abandoned until promoted to an authorized PLAN bullet.

- Victory/defeat presentation polish from v2.64 backlog.
- Title-screen visual-impact pass beyond 7/10.
- Production bundle code splitting.
- Full commercial-AAA content breadth beyond the defined vertical slice.

- Bug: BUILDINGS.nightblade mirrors UNITS.nightblade and can surface a non-placeable unit button in the build menu (needs denormalization audit + regression).
- Dead code fragments (audit P0.017): findTransportNear, findBunkerNear, applyUpgradeTintToBuildings, onProjectileHit — zero call sites; also campaign mission-start does not consume UPKEEP. Bundle into one cleanup ticket with quota ruling.
- P1.029 INCREMENT 1 DONE 2026-09-23: the live variable-dt chain is migrated to fixed 24Hz consumption. __step now runs a fixed accumulator loop (stepSim(TICK): spatial rebuild, unit update/move/fire/harvest, building update, projectile flight, mine/crate/critter, fog/stealth/depletion) + AI/escape/convoy + processTopologyChanges + __simPassive (ultimate charge, surge revert, triggers framework, fog trio, first-contact). Render pass after the loop is presentation-only (verified writer-by-writer: audio, FX pools, overlays, HUD, ambient, coaching). TickAcc clamped to 0.25s (6-tick throttle bug deleted — world ran 2.8x slow before the fix). Golden-replay harness scripts/verify-tick-parity.cjs drives update() directly at 24/48/96Hz-equivalent chunk sizes with __manual rAF isolation + combat seed: 720 ticks x3 paces, identical exportSimState digest, real attrition 18->7 units; wired into run-regression-263 (23/23 green). Natural-play smoke: no errors, units move. NEXT (P1.029 i2): order-execution extraction to pure module + headless move/stop/hold/attack/harvest golden tests; then P1.027 i3 wires input handlers to cmdQueue enqueue + tick-boundary drain (drain site = head of the fixed loop, now live). Interceptor cd + projectile flight dual-clock site is CLOSED (both tick-side).


- P1.027 increment 2 (kernel v2): cmdQueue rewritten to match-scope model (epoch=pause generation, last-intent-wins coalescing per {layer,subject}, enqueue never executes, canonical (tick,player,key) drain). Gate rebuilt on new API: 12/12 (unique-key shuffle identity x4, repeat stability, canonical order, boundary/pause/coalesce semantics, dual-V8 children). Full browser suite re-run GREEN (17/17 + routing 200/200) with new bundle.
- P1.027 INCREMENT 3a DONE 2026-09-23: scene wiring landed. Fixed-tick loop (i1) + cmdQueue enqueue + tick-head drain now live. Converted: RMB rightClickOrder (player-attributed in hotseat, press-time selection snapshot passed at exec), stance Z/X/H/C, stop+siege S, cloak C/K, burrow B, stim F, unload U (transport + bunker emerge), autoMine J, and the four matching HUD buttons (stim/siege/burrow/cloak). Pause bumps matchCmds.epoch -> pre-pause backlog purged; during-pause commands carry the new epoch and execute on resume (kernel semantics verified live, verify-cmd-live 8/8). Selection snapshots are captured at PRESS time and resolved at EXEC time (1-tick later), preserving press semantics.
- P1.027 INCREMENT 3b REMAINING (deferred, honest): patrol placement + cast placements (ult/scan/void/caustic place-at-click), build placement (tryPlace + Ctrl-click group branch), F2-F4 group hotkeys (selection-only, arguably out of sim scope), M summonRadiant, D deployMCV, morph HUD buttons, queueFromHud/queueResearch (production+research queue mutation), autoTeam. Source-scan gate (no state writes in input callbacks) must flip live-scope only after 3b.

- P1.028 state of play: time.delayedCall render-clock timers that MUTATED sim state are deleted (boss spawn, surge buff revert, brood-hatch spawn, 2x endgame win) -> now simTimers deadline entries executed at fixed 24Hz tick boundaries; simTickIndex finally ASSIGNED (exportSimState no longer reads undefined). Remaining delayedCall sites verified presentation-only (fire FX emitters, ack rings, HUD alert flags). Interceptor cd + projectile flight + unit movement still render-dt driven = P1.029 mandated first task, unchanged.
